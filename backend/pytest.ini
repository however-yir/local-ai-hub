[pytest]
addopts = -ra
markers =
    fast: fast unit-style tests that should provide quick feedback
    integration: integration-style tests that exercise API flows and storage backends
    slow: tests that are expected to take longer than average
